[project]
name = "omni-modal-api"
version = "0.1.0"
description = "Python orchestration backend for Omni-Modal Enterprise Research Orchestrator"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
